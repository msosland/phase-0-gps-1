# phase-0-gps-1
git clone - takes a copy of a remote repository and brings it to local machine
touch - adds file
git add - moved file from untracked to staging area
git commit - commits file, saves checkpoint
git push origin master - pushed our local work to our remote repository
git checkout -b -- created a new branch and moved to it


